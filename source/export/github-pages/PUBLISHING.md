# Publishing instructions

See the canonical [PUBLISHING.md](../../../PUBLISHING.md) at the repository root. The existing exporter writes root `dist/`, and GitHub Actions publishes only that directory after checks pass on `main`. Manual ZIP/root-folder publication has been retired.
